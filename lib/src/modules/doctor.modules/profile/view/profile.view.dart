import 'package:flutter/material.dart';

class ProfileViwe extends StatelessWidget {
  const ProfileViwe({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('Profile'),
      ),
      // body: Center(child: ,),
    );
  }
}
